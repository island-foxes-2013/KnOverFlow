<h1>Update Your Answer Here!</h1>

<%= form_for [@answer] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.submit %>
  <% if @answer.errors[:content] %>
    <span class="errors">
      <%= @answer.errors[:content].join(',') %>
    </span>
  <% end %>
<% end %>

<div class='comments_display'>
  <p>Comments:</p>
  <ul>
  <% commentable.comments.each do |comment| %>
    <li>
      <%= comment.content %>&nbsp;
        <span style="float: right;">
          <% if comment.user == current_user %>
            <% if comment.commentable_type == "Question" %>
              <%= button_to "delete", question_comment_path(comment.commentable, comment), {method: :delete} %><%= button_to "edit", edit_question_comment_path(comment.commentable, comment), {method: :get} %>
            <% elsif comment.commentable_type == "Answer" %>
              <%= button_to "delete", answer_comment_path(comment.commentable, comment), {method: :delete} %><%= button_to "edit", edit_answer_comment_path(comment.commentable, comment), {method: :get} %>
            <% end %>
          <% end %>
        </span>
    </li>
  <% end %>
</ul>
</div>

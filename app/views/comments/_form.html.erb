<h4>Post a new Comment!</h4>
<%= form_for [commentable, comment], remote: true do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.submit 'Submit' %>
  <span class="errors"><%= comment.errors[:content].join('') %></span>
<% end %>
